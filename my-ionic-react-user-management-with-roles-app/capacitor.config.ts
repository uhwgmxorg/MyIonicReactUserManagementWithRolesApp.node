import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'my-ionic-react-user-management-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
